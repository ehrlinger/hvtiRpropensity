% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ps-mw-var.R
\name{ps_mw_var}
\alias{ps_mw_var}
\title{Bootstrap variance of a matching-weight treatment effect}
\usage{
ps_mw_var(
  data,
  treatment_col = "tavr",
  outcomes,
  weight_col,
  n_rep = 1000L,
  seed = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{treatment_col}{Name of the group column, coded 0/1 or logical, with
both groups present. Default \code{"tavr"}.}

\item{outcomes}{Character vector of numeric outcome columns.}

\item{weight_col}{Name of the column of matching weights, which must be
positive and finite.}

\item{n_rep}{Number of bootstrap replicates. Default \code{1000}, the macro's
\verb{RESAMPL=}.}

\item{seed}{Optional integer seed.}
}
\value{
An object of class \code{c("ps_mw_var", "ps_data")} with:
\describe{
\item{\verb{$data}}{The input data frame, unchanged.}
\item{\verb{$meta}}{Named list: \code{treatment_col}, \code{weight_col}, \code{outcomes},
\code{method}, \code{n_rep}, \code{seed}, \code{n_total}, \code{n_dropped}.}
\item{\verb{$tables}}{Named list with \code{mw_var}, one row per outcome:
\code{outcome}, \code{label}, \code{sumwgt_0}, \code{mean_0}, \code{sd_0}, \code{sumwgt_1},
\code{mean_1}, \code{sd_1}, \code{estimate}, \code{n_rep}, \code{sd}, \code{p2_5}, \code{p16}, \code{p50},
\code{p84}, \code{p97_5}, \code{z} and \code{p}.}
}
}
\description{
Estimates the weighted difference in means, group 1 minus group 0, for
each outcome, with a bootstrap standard deviation, percentiles, z and p. A
port of the CCF \verb{\%mw_var} SAS macro (Rajeswaran 2014), for matching weights
as described by Li and Greene (2013).
}
\details{
\strong{Estimate.} For each group, \verb{PROC MEANS} weighted summaries: the mean
\verb{sum(w y) / sum(w)}, the SD with an \code{n - 1} denominator, and the sum of
weights. The macro labels the sum of weights \code{N}; it is reported here as
\code{sumwgt_0} and \code{sumwgt_1} because it is not a count. For a 0/1 outcome the
means are weighted proportions.

\strong{Bootstrap.} Each replicate resamples rows with replacement within each
group, to that group's size, and recomputes the weighted means. The
treatment group is the only stratum, as in the macro. \code{sd} is the standard
deviation of the replicate differences, and the percentiles use
\code{quantile(type = 4)}, which is SAS \code{PCTLDEF=1} as the macro sets it.
Replicates where an outcome has no non-missing value in a group are left
out for that outcome, and \code{n_rep} counts those that remain.

\strong{The weights are held fixed.} Each resampled row keeps its original
weight; the propensity model and the weights are not re-estimated. This
reproduces the macro, and it ignores the uncertainty in estimating the
weights, so \code{sd} can be too small.

\strong{Missing values.} Rows with a missing group are dropped. A missing
outcome is dropped for that outcome only.

\strong{Random numbers.} With \code{seed}, the result is reproducible and the
caller's random number stream is restored afterwards.
}
\examples{
dta <- sample_ps_data(n = 200, seed = 42)
dta$mt_wt <- pmin(dta$prob_t, 1 - dta$prob_t) /
  ifelse(dta$tavr == 1, dta$prob_t, 1 - dta$prob_t)
obj <- ps_mw_var(dta, treatment_col = "tavr", outcomes = c("age", "ef"),
                 weight_col = "mt_wt", n_rep = 200, seed = 1)
obj$tables$mw_var

}
\references{
Li L, Greene T. A weighting analogue to pair matching in
propensity score analysis. The International Journal of Biostatistics.
2013;9(2):215-234.
}
\seealso{
\code{\link[=ps_weight]{ps_weight()}}, \code{\link[=ps_stddiff]{ps_stddiff()}}
}
