% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ps-stddiff-perm.R
\name{ps_stddiff_perm}
\alias{ps_stddiff_perm}
\title{Permutation reference for standardized differences}
\usage{
ps_stddiff_perm(
  data,
  treatment_col = "tavr",
  gaussian = NULL,
  nong_ord = NULL,
  binary = NULL,
  categorical = NULL,
  weight_col = NULL,
  reweight = NULL,
  n_perm = 1000L,
  seed = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{treatment_col}{Name of the group column, coded 0/1 or logical, with
both groups present. Default \code{"tavr"}.}

\item{gaussian, nong_ord, binary, categorical}{Character vectors of column
names, by type. See Details. At least one variable is required, and a
variable may appear under one type only. A \code{binary} column must be
coded 0/1 or logical; a \code{categorical} column may have any coding.}

\item{weight_col}{Optional name of the column \code{\link[=ps_stddiff]{ps_stddiff()}} weights by.
Requires \code{reweight}, which supplies its values.}

\item{reweight}{A function of a data frame returning one positive, finite
weight per row, for example one that refits a propensity model and
returns matching weights. Required when \code{weight_col} is given, and
rejected without it.}

\item{n_perm}{Number of permutations. Default \code{1000}, the macro's.}

\item{seed}{Optional integer seed.}
}
\value{
An object of class \code{c("ps_stddiff_perm", "ps_data")} with:
\describe{
\item{\verb{$data}}{The input data frame, unchanged.}
\item{\verb{$meta}}{Named list: \code{treatment_col}, \code{weight_col}, \code{variables},
\code{method}, \code{n_perm}, \code{seed}, \code{n_total}.}
\item{\verb{$tables}}{Named list with \code{stddiff_perm}, one row per variable:
\code{variable}, \code{label}, \code{type}, \code{observed} (as \code{\link[=ps_stddiff]{ps_stddiff()}} reports it)
and the permutation percentiles \code{p2_5}, \code{p16}, \code{p50}, \code{p84},
\code{p97_5}.}
}
}
\description{
Computes \code{\link[=ps_stddiff]{ps_stddiff()}} on the observed groups and on \code{n_perm} random
permutations of the group labels, and reports the observed value beside
percentiles of its permutation distribution. A port of the CCF
\verb{\%stddiffci} SAS macro (Artis 2020).
}
\details{
\strong{What the percentiles are.} They describe the standardized difference
you would see if group membership carried no information about the
variable. They are not a confidence interval for the observed value,
whatever the SAS macro's name suggests: an observed difference outside the
2.5 to 97.5 range is larger than label shuffling alone tends to produce.
The 16 and 84 percentiles bound the central 68\%, the range the macro's 2021
correction fixed.

\strong{Percentile definition.} \code{quantile(type = 2)}, which is SAS
\code{PCTLDEF=5}, the \verb{PROC UNIVARIATE} default the macro relies on.
Permutations where a variable's difference is \code{NA} are left out of that
variable's percentiles.

\strong{Weights.} A matching or IPTW weight depends on the group, so a weight
carried over unchanged onto permuted groups describes neither the observed
design nor the null. \code{reweight} must therefore be given whenever
\code{weight_col} is: a function taking a data frame and returning one weight
per row. It is called once on the unpermuted data, for the observed row,
and once on each permuted data set, and its result is written to
\code{weight_col} before \code{\link[=ps_stddiff]{ps_stddiff()}} runs. The macro instead expects the
permuted weights to exist as columns before it is called.

\strong{Missing groups.} Rows with a missing group keep it and are not
permuted into either group.

\strong{Random numbers.} With \code{seed}, the permutations are reproducible and the
caller's random number stream is restored afterwards. Without it, the
current stream is used and advanced.
}
\examples{
dta <- sample_ps_data(n = 200, seed = 42)
obj <- ps_stddiff_perm(dta, treatment_col = "tavr", gaussian = c("age", "ef"),
                       binary = "female", n_perm = 200, seed = 1)
obj$tables$stddiff_perm

}
\seealso{
\code{\link[=ps_stddiff]{ps_stddiff()}}
}
