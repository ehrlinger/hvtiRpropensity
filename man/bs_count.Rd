% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bs-score.R
\name{bs_count}
\alias{bs_count}
\title{Estimate a balancing score via negative-binomial or Poisson regression}
\usage{
bs_count(
  formula,
  data,
  outcome_col = NULL,
  id_col = "id",
  imputation_col = NULL,
  dist = c("negbin", "poisson"),
  score_col = "bs",
  n_strata = 10L,
  strata_col = "cluster",
  create_binary_strata = TRUE,
  covariates = NULL
)
}
\arguments{
\item{formula}{A formula with the count outcome on the LHS,
e.g. \code{rbc_tot ~ female + agee + in_hct + hx_csurg + iv_cpb}.}

\item{data}{A data frame, or a stacked MI data frame when
\code{imputation_col} is set.}

\item{outcome_col}{Name of the count outcome column.  \code{NULL}
(default) extracts from \code{formula}.}

\item{id_col}{Patient identifier column.  Required when
\code{imputation_col} is set.  Default \code{"id"}.}

\item{imputation_col}{Imputation-index column.  \code{NULL} (default)
means a single complete dataset.}

\item{dist}{Distribution for the GLM.  \code{"negbin"} (default)
calls \code{\link[MASS:glm.nb]{MASS::glm.nb()}} (negative binomial, log link);  \code{"poisson"} calls
\code{\link[stats:glm]{stats::glm()}} with \code{family = poisson(link = "log")}.}

\item{score_col}{Name of the output balancing score column
(averaged linear predictor on the log scale).  Default \code{"bs"}.}

\item{n_strata}{Number of strata.  Default \code{10L}.}

\item{strata_col}{Name of the cluster/stratum column.  Default
\code{"cluster"}.}

\item{create_binary_strata}{Logical.  If \code{TRUE} (default), binary
indicator columns \code{stra_1} through \verb{stra_<n_strata>} are appended.}

\item{covariates}{Covariate columns for diagnostics.}
}
\value{
An object of class \code{c("bs_count", "ps_data")} with:
\describe{
\item{\verb{$data}}{Base data frame with \code{score_col}, \code{"quintile"},
\code{"decile"}, \code{strata_col}, and (if \code{create_binary_strata})
\code{stra_1}...\code{stra_k} appended.}
\item{\verb{$meta}}{Named list: \code{formula}, \code{outcome_col}, \code{id_col},
\code{imputation_col}, \code{score_col}, \code{dist}, \code{n_strata}, \code{strata_col},
\code{bundle_version}, \code{model_family}, \code{package_versions}, \code{method},
\code{n_imputations}, \code{n_total}.}
\item{\verb{$tables}}{Named list: \code{strata_counts}.}
}
}
\description{
Fits a GLM with a count distribution (\code{\link[MASS:glm.nb]{MASS::glm.nb()}} for negative
binomial, or \code{\link[stats:glm]{stats::glm()}} with Poisson family) on a (possibly
multiply-imputed) dataset, averages the per-patient \strong{linear predictors}
(log-scale, i.e. xbeta) across imputations, and assigns rank-based strata.
Mirrors \code{tp.pm.count.balncing_score.sas} (PROC GENMOD \verb{dist=nb link=log}
\itemize{
\item PROC SUMMARY + the \verb{\%grp} macro).
}
}
\details{
The linear predictor (xbeta, log-scale) rather than the fitted count is
used as the balancing score because it is on an unbounded continuous scale
more suitable for rank-based stratification.
}
\section{Package dependency}{

\code{dist = "negbin"} (the default) requires the \pkg{MASS} package (a
recommended R package, usually pre-installed).  Install with
\code{install.packages("MASS")} if missing, or use \code{dist = "poisson"}.
}

\examples{
# Mirrors the SAS template workflow:
#   PROC GENMOD dist=nb link=log (negative binomial for overdispersed counts)
#   xbeta (log-scale linear predictor) used as the balancing score
#   (not the fitted count -- log scale is more suitable for stratification).
# The \%grp macro binary indicators stra_1 ... stra_k are created when
# create_binary_strata = TRUE (the default).
\donttest{
dta <- sample_ps_data_count(n = 300, seed = 42)

# Poisson fallback (no MASS dependency)
obj <- bs_count(
  rbc_tot ~ female + age + diabetes + hypertension,
  data = dta,
  dist = "poisson"
)
print(obj)
table(obj$data$cluster)    # 10 equal-ish strata by default

# Binary stratum indicators mirror the SAS \%grp macro output
head(obj$data[, c("id", "bs", "cluster", "stra_1", "stra_2", "stra_10")])

# Negative binomial (accounts for overdispersion; requires MASS)
if (requireNamespace("MASS", quietly = TRUE)) {
  obj_nb <- bs_count(
    rbc_tot ~ female + age + diabetes + hypertension,
    data = dta,
    dist = "negbin"
  )
  print(obj_nb)
}
}

}
\seealso{
\code{\link[=bs_continuous]{bs_continuous()}}, \code{\link[=ps_logistic]{ps_logistic()}}
}
