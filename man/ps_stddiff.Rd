% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ps-stddiff.R
\name{ps_stddiff}
\alias{ps_stddiff}
\title{Standardized differences between two groups}
\usage{
ps_stddiff(
  data,
  treatment_col = "tavr",
  gaussian = NULL,
  nong_ord = NULL,
  binary = NULL,
  categorical = NULL,
  weight_col = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{treatment_col}{Name of the group column, coded 0/1 or logical, with
both groups present. Default \code{"tavr"}.}

\item{gaussian, nong_ord, binary, categorical}{Character vectors of column
names, by type. See Details. At least one variable is required, and a
variable may appear under one type only. A \code{binary} column must be
coded 0/1 or logical; a \code{categorical} column may have any coding.}

\item{weight_col}{Optional name of a column of positive weights, for
example matching weights from \code{\link[=ps_weight]{ps_weight()}}. Weights must be finite.}
}
\value{
An object of class \code{c("ps_stddiff", "ps_data")} with:
\describe{
\item{\verb{$data}}{The input data frame, unchanged.}
\item{\verb{$meta}}{Named list: \code{treatment_col}, \code{weight_col}, \code{variables}
(a list by type), \code{method}, \code{n_total}, \code{n_dropped}.}
\item{\verb{$tables}}{Named list with \code{stddiff}, a data frame with one row
per variable in the order given: \code{variable}, \code{label} (the column's
\code{"label"} attribute, or \code{NA}), \code{type} (\code{"gaussian"}, \code{"nong_ord"},
\code{"binary"}, \code{"categorical"} or \code{"onelevel"}) and \code{stddiff}, which is
\code{NA} when the pooled variance is zero.}
}
}
\description{
Computes the standardized difference, group 1 minus group 0, for each
named variable, choosing the formula by the variable's type. A port of the
CCF \verb{\%stddiff} SAS macro (Artis 2019, from Yang 2012), and intended to
reproduce its \code{est.stddiff} output.
}
\details{
\strong{Variable types.} The type decides the formula, so it is given, not
inferred: an ordinal NYHA class stored as an integer would otherwise be
treated as Gaussian.
\itemize{
\item \code{gaussian}: difference in means over \code{sqrt((var1 + var0) / 2)}. The
denominator averages the two variances; it does not pool them by sample
size, so it differs from a Cohen's d when the groups are unequal.
\item \code{nong_ord}: the same formula on ranks of the pooled sample, ties
averaged, as \verb{PROC RANK} does by default. Ranks are unweighted; only
their means and variances use \code{weight_col}.
\item \code{binary} and \code{categorical} are routed by the number of levels observed,
as the macro routes its \verb{BINARY=} and \verb{CATG=} lists: one level gives 0,
two use the binary formula on the proportion at the last level in sort
order (\code{1} for a 0/1 variable, the last level for a factor), and more than two use the Mahalanobis
form of Yang and Dalton (2012). For that form the last level in sort
order is the one dropped, and \code{S} has diagonal
\code{(t_i (1 - t_i) + c_i (1 - c_i)) / 2} and off-diagonal
\verb{-(t_i t_j + c_i c_j) / 2}. A categorical difference is never negative.
When \code{S} is singular, as when the two groups share no levels, the
difference is \code{NA} with a warning naming the variable.
}

\strong{Weights.} Means are \verb{sum(w x) / sum(w)}; variances are
\code{sum(w (x - m)^2) / (n - 1)}, which is what \verb{PROC MEANS} reports with a
\code{WEIGHT} statement. Weights must be positive.

\strong{Missing values.} Rows with a missing group are dropped. Otherwise a
missing value is dropped for that variable only, and a variable with no
non-missing values gets \code{NA}.

\strong{Units.} The result is a proportion of a standard deviation, not a
percent. The SAS output labels the column "(\%)" but does not multiply by
100.
}
\examples{
dta <- sample_ps_data(n = 200, seed = 42)
obj <- ps_stddiff(dta, treatment_col = "tavr",
                  gaussian = c("age", "ef"), binary = c("female", "diabetes"))
obj$tables$stddiff

}
\references{
Yang D, Dalton JE. A unified approach to measuring the effect
size between two groups using SAS. SAS Global Forum 2012, paper 335-2012.
}
\seealso{
\code{\link[=ps_match]{ps_match()}}, \code{\link[=ps_weight]{ps_weight()}}
}
