% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ps-forest.R
\name{ps_forest}
\alias{ps_forest}
\title{Random-forest propensity score (out-of-bag)}
\usage{
ps_forest(
  formula,
  data,
  treatment_col = NULL,
  id_col = "id",
  score_col = "prob_t",
  logit_col = "logit_t",
  weight_col = "mt_wt",
  treated_level = NULL,
  ntree = 500L,
  seed = NULL,
  clip = 0.001,
  ...
)
}
\arguments{
\item{formula}{Two-sided formula, \code{treatment ~ x1 + x2 + ...}.}

\item{data}{Data frame with no missing values in the formula variables.}

\item{treatment_col}{Treatment column name; defaults to the formula
response. Binary, 0/1 or logical.}

\item{id_col}{Optional patient identifier column, carried through.}

\item{score_col, logit_col, weight_col}{Names of the appended score, logit and
overlap-weight columns.}

\item{treated_level}{Value of \code{treatment_col} that is "treated" (the score
is P(treated)). Defaults to \code{1} / \code{TRUE}.}

\item{ntree}{Number of trees.}

\item{seed}{Optional integer seed passed to \code{\link[randomForestSRC:rfsrc]{randomForestSRC::rfsrc()}}.}

\item{clip}{Scores are clipped to \verb{[clip, 1 - clip]} before the logit and
weight are computed. Forest probabilities can be exactly 0 or 1.}

\item{...}{Further arguments for \code{\link[randomForestSRC:rfsrc]{randomForestSRC::rfsrc()}}.}
}
\value{
A \code{ps_forest} / \code{ps_data} object. \verb{$data} is \code{data} plus
\code{score_col}, \code{logit_col}, \code{weight_col} (overlap weights), \code{quintile} and
\code{decile}; \verb{$meta} follows \code{\link[=ps_logistic]{ps_logistic()}} with \code{method = "forest-oob"};
\verb{$tables} holds \code{smd} and \code{group_counts}; \verb{$models$forest} is the fitted
forest.
}
\description{
Estimates P(treated) with a random-forest classifier
(\code{\link[randomForestSRC:rfsrc]{randomForestSRC::rfsrc()}}) and returns the \strong{out-of-bag} class
probability as the score, so no patient is scored by trees that saw them.
The result has the same contract as \code{\link[=ps_logistic]{ps_logistic()}}, so it feeds
\code{\link[=ps_weight]{ps_weight()}}, \code{\link[=ps_match]{ps_match()}}, \code{\link[=sa_overlap]{sa_overlap()}} and
\code{hvtiPlotR::hv_balance()} / \code{hvtiPlotR::hv_mirror_hist()} unchanged.
}
\examples{
\donttest{
if (requireNamespace("randomForestSRC", quietly = TRUE)) {
  dta <- sample_ps_data(n = 150, seed = 1)[, c("id", "tavr", "age", "ef")]
  obj <- ps_forest(tavr ~ age + ef, dta, ntree = 100, seed = 1)
  summary(obj$data$prob_t)
}
}
}
