% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ps-score.R
\name{ps_nominal}
\alias{ps_nominal}
\title{Estimate propensity scores via nominal (generalised logit) logistic
regression}
\usage{
ps_nominal(
  formula,
  data,
  treatment_col = NULL,
  id_col = "id",
  imputation_col = NULL,
  ref_level = NULL,
  score_col_prefix = "prob",
  trace = FALSE,
  covariates = NULL,
  treatment_levels = NULL
)
}
\arguments{
\item{formula}{A formula with the nominal treatment on the LHS,
e.g. \code{rtyp ~ age + female + ef + diabetes}.}

\item{data}{A data frame (single or stacked MI).}

\item{treatment_col}{Name of the nominal treatment column.  \code{NULL}
(default) extracts from \code{formula}.}

\item{id_col}{Patient identifier column.  Default \code{"id"}.}

\item{imputation_col}{Imputation-index column.  \code{NULL} (default) means
a single complete dataset.}

\item{ref_level}{Reference level for the multinomial model.  \code{NULL}
(default) uses the first factor level, matching \code{REF=first} in SAS.}

\item{score_col_prefix}{Prefix for per-level output columns (\verb{prob_<level>}
by default).  Default \code{"prob"}.}

\item{trace}{Logical.  If \code{FALSE} (default), suppresses
\code{\link[nnet:multinom]{nnet::multinom()}} iteration messages.}

\item{covariates}{Covariate columns for diagnostics.}

\item{treatment_levels}{Complete nominal treatment levels. \code{NULL} preserves
the levels inferred by the historical interface.}
}
\value{
An object of class \code{c("ps_nominal", "ps_data")} with:
\describe{
\item{\verb{$data}}{Base data frame with one \verb{prob_<level>} column per
treatment level appended.}
\item{\verb{$meta}}{Named list: \code{formula}, \code{treatment_col}, \code{id_col},
\code{imputation_col}, \code{score_cols}, \code{levels}, \code{ref_level}, \code{method},
\code{n_imputations}, \code{n_total}.}
\item{\verb{$tables}}{Named list: \code{group_counts}.}
}
}
\description{
Fits a multinomial logistic regression model via \code{\link[nnet:multinom]{nnet::multinom()}} for a
nominal (unordered) treatment variable, equivalent to SAS
\verb{PROC LOGISTIC ... / link=glogit}.  One probability column per treatment
level is appended to the dataset.
}
\details{
When \code{imputation_col} is supplied, models are fit on each imputed dataset
and per-level probabilities are averaged per patient across imputations.
}
\section{Package dependency}{

Requires \pkg{nnet} (a recommended R package, usually pre-installed).
Install with \code{install.packages("nnet")} if missing.
}

\examples{
# Mirrors the SAS template workflow:
#   PROC LOGISTIC ... / link=glogit  (generalised logit)
# The first factor level is used as the reference category, matching
# SAS REF=first.  Change ref_level to match a different reference.
\donttest{
dta <- sample_ps_data_nominal(n = 300, seed = 42)
obj <- ps_nominal(
  rtyp ~ age + female + ef + diabetes,
  data = dta
)
print(obj)

# One probability column per treatment level (analogous to p_cos, p_per,
# p_dev, p_ce from the PROC TRANSPOSE step in the SAS template).
head(obj$data[, c("id", "rtyp", "prob_COS", "prob_PER", "prob_DEV", "prob_CE")])

# Explicitly set a different reference level
obj_ce <- ps_nominal(
  rtyp ~ age + female + ef + diabetes,
  data      = dta,
  ref_level = "CE"    # matches REF=last in SAS
)
}

}
\seealso{
\code{\link[=ps_logistic]{ps_logistic()}}, \code{\link[=ps_ordinal]{ps_ordinal()}}, \code{\link[=ps_match]{ps_match()}}
}
