% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ps-support.R
\name{ps_support}
\alias{ps_support}
\title{Compare support (overlap) rules}
\usage{
ps_support(x, flags = list(), trim = c(0.1, 0.9), common = TRUE)
}
\arguments{
\item{x}{A \code{ps_data} object with a score and treatment column.}

\item{flags}{Optional named list of extra logical vectors, one element per
row of \code{x$data}, \code{TRUE} meaning the rule would drop that patient.}

\item{trim}{Length-2 numeric, or \code{NULL}. Adds the rule "score outside
\verb{[trim[1], trim[2]]}". Default \code{c(0.1, 0.9)}.}

\item{common}{Logical; add the rule "score outside the range occupied by
both groups" (common support).}
}
\value{
A \code{ps_support} / \code{ps_data} object. \verb{$data} is \code{x$data} plus one
logical column \verb{weak_<rule>} per rule. \verb{$tables$counts} gives the number
flagged per rule; \verb{$tables$agreement} gives, for every pair of rules,
\code{n_both}, \code{n_neither}, \code{pct_agree}, Cohen \code{kappa} and \code{jaccard} (overlap
of the two dropped sets). \verb{$meta$common_support} is the interval used.
}
\description{
Flags patients with weak support under several rules and measures how well
the rules agree. Built-in rules use the propensity score of a \code{ps_data}
object; any other rule (for example an isolation-forest tail flag) can be
supplied as a logical vector.
}
\examples{
dta <- sample_ps_data(n = 200, seed = 3)[, c("id", "tavr", "age", "ef")]
sup <- ps_support(ps_logistic(tavr ~ age + ef, dta))
sup$tables$agreement
}
