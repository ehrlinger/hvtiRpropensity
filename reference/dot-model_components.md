# Extract normalized coefficients and covariance from a fitted model

Extract normalized coefficients and covariance from a fitted model

## Usage

``` r
.model_components(fit, family)
```

## Arguments

- fit:

  Fitted model object.

- family:

  Model family.

## Value

A list with `estimate` and `covariance`.
