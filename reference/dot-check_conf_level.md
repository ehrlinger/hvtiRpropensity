# Validate an inference confidence level

Validate an inference confidence level

## Usage

``` r
.check_conf_level(conf_level)
```

## Arguments

- conf_level:

  Confidence level.

## Value

Invisible `NULL`.
